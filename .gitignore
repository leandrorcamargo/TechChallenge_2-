# Enunciado do desafio (não versionar)
*.pdf

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# Dados locais
# Obs: a pasta data/ É versionada (dados baixados em formato comprimido).
# Ignoramos apenas dados descompactados/derivados para nao inchar o repo.
*.parquet
*.db
data/**/*.csv
!data/*.csv.gz

# Data lake local (mirror do S3 gerado pelos jobs) - nao versionar
lake/

# Spark / PySpark
spark-warehouse/
metastore_db/
derby.log
*.crc
_SUCCESS

# Terraform (infra/)
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
projeto/infra/build/

# Credenciais / segredos
.env
*.key
*.pem
credentials.json
service-account*.json

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
