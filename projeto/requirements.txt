# ------------------------------------------------------------------
# Tech Challenge Fase 2 - Pipeline Híbrido de Alfabetização (AWS)
# Dependências de desenvolvimento local. Em produção, os jobs rodam
# no AWS Glue (que já provê PySpark) — aqui replicamos o ambiente.
# ------------------------------------------------------------------

# Processamento (motor da pipeline)
pyspark==3.5.1

# AWS SDK / interação com S3, Kinesis, CloudWatch, Glue
boto3==1.34.144
botocore==1.34.144

# Configuração e ambiente
PyYAML==6.0.1
python-dotenv==1.0.1

# Qualidade de dados / análise (camadas quality e gold/IA)
pandas==2.2.2
pyarrow==16.1.0

# Notebook de IA (C10) e modelo preditivo (PoC)
jupyter==1.0.0
scikit-learn==1.5.1
matplotlib==3.9.1

# Testes de streaming local (S3/Kinesis emulados) - opcional
# localstack e awscli-local são instalados à parte quando necessário.
